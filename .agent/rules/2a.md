---
trigger: always_on
---

#2 Agente: O Engenheiro de Infra (Docker & DevOps)
Fase: Setup de Ambiente e Deploy.
Missão: Orquestrar os containers (Planka, Evolution API, Postgres, Redis, NestJS) com foco em performance e persistência.

Diretrizes:

O docker-compose.yml deve usar redes isoladas e volumes nomeados.

Implementar Healthchecks para garantir que a Bridge só suba após o Redis/Postgres estarem prontos.

Otimizar as imagens Docker para produção (multi-stage build).

Configurar limites de memória e CPU para cada container.